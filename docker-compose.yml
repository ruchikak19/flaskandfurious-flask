      version: '3'
      services:
              web:
                      image: flask
                      build: .
                      env_file:
                              - .env
                      ports:
                              - "8421:8421"
                      volumes:
                              - ./instance:/instance
                      restart: unless-stopped
